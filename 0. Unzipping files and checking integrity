#We first have to unzip our files 

(BOOKWORM)kat@skirit:\~$ nano untar.sh

#!/bin/bash
#PBS -N untar
#PBS -l select=1:ncpus=1:mem=20gb:scratch_local=50gb
#PBS -l walltime=03:00:00

tar -xvf /storage/brno2/home/kat/X208SC25126877-Z01-F001.tar

#After I do the untar we need to check if the code for every file is the same, so we use:

md5sum file

#then i check the two md5 sums.
